const wrapper = require('../src/rules-wrapper');

const {
    expect,
    describe,
    it,
    jest: _jest,
} = require('@jest/globals');

describe('support auth0 embedded object', () => {
    it('have auth0 object with valid access_token', () => {

    });

});